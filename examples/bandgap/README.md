# Bandgap Reference Example — 1.25V PTAT+CTAT, Sky130 PDK

Curvature-corrected bandgap with <20 ppm/K temperature coefficient.

## Commands

```bash
spec-parser aggregate corners/ --spec spec.yaml
spec-parser aggregate corners/ --spec spec.yaml --output bg_corners.html
```
